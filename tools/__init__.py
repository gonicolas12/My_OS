"""Couche 2 — outils MCP exposés au modèle.

Chaque outil hérite de :class:`tools.base_tool.BaseTool` et déclare son
``risk_level``. Un outil sans niveau déclaré n'est pas chargeable
(cf. docs/SECURITY.md §4).
"""
